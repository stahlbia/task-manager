meta {
  name: get from user4
  type: http
  seq: 8
}

get {
  url: http://{{task-manager.com}}/api/v1/tasks?assigned_to={{user_id_4}}
  body: none
  auth: none
}

params:query {
  assigned_to: {{user_id_4}}
}

headers {
  authorization: {{accessToken}}
}
